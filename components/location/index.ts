/**
 * Location Components Export
 *
 * Centralized export of all location-related components
 */

// Export the client wrapper instead of the direct component
// This ensures proper usage in Server Components
export { LocationConsentWrapper as LocationConsent } from "./location-consent-wrapper";
